<h1>Checkout Page</h1>

<%= @carted_products.each do |carted_product| %>
  <h2><%= carted_product.product.product_type %> -Quantity <%= carted_product.quantity %></h2>
<% end %> 

<%= link_to "Buy Now", "/orders", method: :post class="btn btn-primary" %>
